fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
python-dotenv>=1.0.0
presidio-analyzer>=2.2.0
spacy>=3.8.14
# Anthropic SDK — used by the PoC scripts (demo_claude.py, demo_html.py,
# diagnose_upstream.py) and by the example clients under examples/. Adding
# it here so a single `./setup.sh` lands a fully working environment.
anthropic>=0.30.0
